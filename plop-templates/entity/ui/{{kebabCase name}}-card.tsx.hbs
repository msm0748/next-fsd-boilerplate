import { Box } from '@chakra-ui/react';
import type { {{pascalCase name}}DTO } from '../model/{{kebabCase name}}.types';

interface Props {
  entity: {{pascalCase name}}DTO;
}

// 엔티티 데이터를 받아 보여주는 순수한 UI 컴포넌트
export const {{pascalCase name}}Card = ({ entity }: Props) => {
  return (
    <Box></Box>
  );
};